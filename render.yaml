# render.yaml
services:
  - type: web
    name: marathon-streamlit
    env: python
    plan: free
    buildCommand: ""
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0
